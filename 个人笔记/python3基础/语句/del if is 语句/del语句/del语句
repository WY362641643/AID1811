
del　语句
　　　作用：用于删除变量，同时解除与对象的关联

　列表
    用于删除列表中的元素　　
    语法:
      del 列表[索引]
      del 列表[切片]
    示例:
      L = [1, 3.14, 2, 100, 3]
      del L[1]
      del L[2]
      print(L)
    说明:
      列表中的元素被删除,后续元素的索引值会变小,即后续元素前移
　
　字典
    删除字典的键,同时解除与值的绑定关系
    语法:
      del 字典[键]
    示例:
      d= {'name': 'tarena', 'age':15}
      del d['name']  # 删除'name'键