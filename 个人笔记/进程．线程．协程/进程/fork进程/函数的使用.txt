
二、进程相关函数使用
    
    os.getpid()
    功能：   获取当前进程的PID号
    返回值：  返回PID号

    os.getppid()
    功能:   获取父进程的PID号
    返回值： 返回 父进程PID号

    os._exit(status)
    功能：  退出一个进程
    参数：  表示进程退出状态 

    sys.exit([status])
    功能： 退出一个进程
    参数： 默认为0 整数表示进程退出状态 
           字符串 则在进程退出时打印该字符串